import React from 'react';
import {Grid} from "@mui/material";

const Home = () => {
    return (
        <Grid container spacing={2}>
            <Grid item xs={8}>

            </Grid>
        </Grid>
    );
};

export default Home;